# 底盘独立测试

此版本只启动 CAN1（四个底盘 M2006，ID `0x201`～`0x204`），不会启动 CAN2、升降、伸缩、舵机、视觉或原自动流程。

`MDK-ARM/PID_task.h` 中的 `CHASSIS_TEST_MODE` 当前为 `1`，所以编译出的就是测试固件。测试完成后请把它改为 `0`，再编译回正常固件。

## 上电测试步骤

1. 将车架垫起或清空前后左右至少 1 米范围；确认急停/断电方式可用。
2. 编译并烧录本工程，然后上电。
3. 按住板载 PE2 按钮约 1 秒，松开按钮。
4. 程序等待 2 秒后，依次以低速测试：前进、后退、左移、右移、逆时针旋转、顺时针旋转；每个动作 2 秒，中间停车 1 秒。
5. 任何动作中再次按下 PE2，会在当前 2 ms 控制周期内发送四路零电流并锁定停机；需要复位后才可重新开始。

## 可调参数

`MDK-ARM/chassis_test.c` 顶部：

- `TEST_SPEED_RPM`：直线动作速度，当前 `1000 rpm`。
- `TEST_ROTATE_RPM`：旋转动作速度，当前 `700 rpm`。
- `TEST_CURRENT_LIMIT`：单电机电流命令上限，当前 `2500`，明显低于 M2006 的工程上限 `10000`。

如果前后或左右方向与注释相反，不改接线，先修改 `TestCommandByTime()` 中相应轴的正负号确认底盘坐标系。
